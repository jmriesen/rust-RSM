[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
lang-model = {path = "../lang-model/"}
value = {path = "../value/"}
ir = {path = "../ir/"}
thiserror = "1.0.63"
mutants = "0.0.3"
chumsky = {workspace=true, features = ["debug"]}
ariadne = "0.6.0"

[dev-dependencies]
insta = { version = "1.48.0", features = ["yaml"] }
